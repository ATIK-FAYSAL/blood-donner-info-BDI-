<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.atik_faysal.bdi_">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE"></uses-permission>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/app_icon"
        android:label="BDI"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".add_user"
            android:label="Add New Donner"/>
        <activity
            android:name=".home_page"
            android:label="Donate Blood"
            android:theme="@style/AppTheme"></activity>
        <activity android:name=".rulls"></activity>
        <activity android:name=".update_information"
            android:label="Update Information"></activity>
        <activity android:name=".change_password"
            android:label="Change Password"></activity>
        <activity android:name=".delete_class"></activity>
        <activity android:name=".feedback"
            android:label="Feedback"></activity>
        <activity android:name=".person_show"></activity>
        <activity android:name=".PersonInformation"
            android:label="Donner Information"></activity>
        <activity android:name=".set_date"
            android:label="Set last donate date"></activity>
        <activity android:name=".About_me"
            android:label="About me"></activity>
    </application>

</manifest>